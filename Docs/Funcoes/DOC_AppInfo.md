# DOC_AppInfo

**Ficheiro:** [`lib/app-info.ts`](../../lib/app-info.ts)

- `APP_VERSION` — importa versão de [`package.json`](../../package.json) via `@/package.json` (`resolveJsonModule`).
- `LEGAL_VERSION` — string fixa para aceitação de termos (`2026.04`).

Usado em sidebar e fluxos legais/perfil.
