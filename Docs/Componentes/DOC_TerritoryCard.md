# DOC_TerritoryCard

**Ficheiro:** [`components/territory/territory-card.tsx`](../../components/territory/territory-card.tsx)

Props: `territory`, `isOwn`, `isSelected`, `onClick`.

Mostra estado (`active`, `protected`, `disputed`, `expired`), nível de dominância (bronze–diamond), área formatada.
