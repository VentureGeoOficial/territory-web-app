# DOC_MarketingLanding

**Ficheiro:** [`components/home/marketing-landing.tsx`](../../components/home/marketing-landing.tsx)

Landing longa (marketing): benefícios, features, CTAs, redes sociais, `useInstallPrompt` para PWA. Centenas de linhas — estrutura repetitiva de secções + ícones lucide.

Principais ações: links para `/login`, `/cadastro`, botão instalar PWA quando `canInstall`.
