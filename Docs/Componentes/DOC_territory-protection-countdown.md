# DOC_territory-protection-countdown

**Ficheiro:** [`components/territory/territory-protection-countdown.tsx`](../../components/territory/territory-protection-countdown.tsx)

## Objetivo

Exibir **Proteção restante:** com atualização automática (tick 30s) para territórios em estado visual `protected`.

## Props

- `protectedUntil` — epoch ms (Firestore)
- `displayKind` — de `getTerritoryDisplayStatus`
- `compact` — layout reduzido para cards

## Logs

Sem logs dedicados (componente puramente visual).
