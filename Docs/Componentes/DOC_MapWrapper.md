# DOC_MapWrapper

**Ficheiro:** [`components/map/map-wrapper.tsx`](../../components/map/map-wrapper.tsx)

- `dynamic(..., { ssr: false })` para `TerritoryMap` e `MapControlsOverlay` (Leaflet sem SSR).
- Loading state com `Spinner` + texto "Carregando mapa...".
