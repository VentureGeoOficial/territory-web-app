# DOC_AppError

**Ficheiro:** [`app/error.tsx`](../../app/error.tsx)

Error boundary App Router; `useEffect` faz `console.error('[v0] Error caught:', error)`.

UI: mensagem `error.message`, botão `reset`.
