/**
 * Preferências de conta e perfil: camada estável para telas (`conta`).
 */
export {
  getUserProfile,
  recordLegalAcceptance,
  updateUserProfile,
} from '@/lib/firebase/user-profile'

export { saveNotificationPreferences } from '@/lib/firebase/notification-preferences'
